Your goal is to design the UI elemenets from the image provided. Follow the instructions below:
- carefullt analyze the image and identify the UI elements such as buttons, text fields, images, etc.
- understand the layout and structure of the UI, including the placement and alignment of elements.
- use appropriate Flutter widgets to recreate the UI elements and layout.
- do not copy the colors, fonts, or any other design aspects from the image. Focus solely on the structure and functionality of the UI.