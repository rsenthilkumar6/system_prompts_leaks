# CREATE DESIGN VARIATIONS

UI TO BUILD: $ARGUMENTS

## Goal

Your goal is to create UI with variations, so that users can review and decice which one is better.

## Task

- Analyze the style guide in `design-system.json`, as well as mockups for reference.
- Build one single html page of just one screen to build a UI based on user's feedback/task.
- output html in ui_iterations folder as ui_{n}.html (where n needs to be unique like ui_1.html, ui_2.html, etc.,)